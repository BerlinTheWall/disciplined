{
  "permissions": {
    "allow": [
      "Bash"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; git add \"$f\" 2>/dev/null && git commit -m \"chore: auto-commit $f\" 2>/dev/null || true; }",
            "statusMessage": "Auto-committing changes..."
          }
        ]
      }
    ]
  }
}

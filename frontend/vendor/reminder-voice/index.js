// JS entry is intentionally empty: the app registers the plugin itself via
// registerPlugin("ReminderVoice") in src/lib/reminderAudio.ts. This package
// exists to carry the native Swift implementation into the iOS build.
module.exports = {};

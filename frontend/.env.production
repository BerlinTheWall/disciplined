# Used by `vite build` (production mode). The packaged iOS app talks to the
# live Railway backend. Origin only — API paths already prepend /api/.
VITE_API_URL=https://disciplined-production-807b.up.railway.app
